# required vars:
# - uid
# - width
# - height
# - icon_on
# - icon_off
# - text

# Mushroom-style fan button layout with green color
# button:
  id: ${uid}_widget
  width: ${width}
  height: ${height}
  pad_all: 12
  widgets:
    # Icon circle container - OFF state (gray)
    - obj:
        id: ${uid}_icon_circle_off
        width: 50
        height: 50
        radius: 25
        bg_color: ${color_surface_alt}
        border_width: 0
        align: LEFT_MID
        scrollbar_mode: "off"
        hidden: true
        widgets:
          - label:
              text: ${icon_off}
              text_font: mdi_md
              text_color: ${color_icon_muted}
              align: CENTER
    # Icon circle container - ON state (green)
    - obj:
        id: ${uid}_icon_circle_on
        width: 50
        height: 50
        radius: 25
        bg_color: ${color_tint_green}
        border_width: 0
        align: LEFT_MID
        scrollbar_mode: "off"
        hidden: true
        widgets:
          - label:
              text: ${icon_on}
              text_font: mdi_md
              text_color: ${color_fan_on}
              align: CENTER
    # Icon circle container - UNKNOWN state
    - obj:
        id: ${uid}_icon_circle_unknown
        width: 50
        height: 50
        radius: 25
        bg_color: ${color_surface_alt}
        border_width: 0
        align: LEFT_MID
        scrollbar_mode: "off"
        widgets:
          - label:
              text: "\U000F01D8"
              text_font: mdi_md
              text_color: ${color_icon_muted}
              align: CENTER
    # Text label to the right of the circle
    - label:
        id: ${uid}_widget_text
        text: ${text}
        text_font: roboto_md
        align: LEFT_MID
        x: 62
