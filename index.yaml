esphome:
  name: esphome-web-e90dec
  friendly_name: HA UI

substitutions:
  home_page: bedroom
  # Design tokens. Defined at the top level so every page, widget, theme and
  # lambda in the tree can reference them as ${color_*}.
  <<: !include layouts/tokens.yaml

packages:
  common: !include common.yaml
  device: !include device.yaml
  layout: !include layouts/main.yaml
